# Domain lessons

Non-obvious **business rules** and domain invariants — the kind of thing a competent developer would still get wrong without being told, because it's a decision this project made, not a general programming fact. Append-only — add new entries at the top. Each entry is dated.

> Only generate/keep this file if the project has a non-trivial domain (business rules that aren't obvious from the code alone — pricing logic, permission models, workflow states, domain-specific vocabulary). A pure technical tool/library/CLI usually doesn't need it — delete this file in that case rather than leaving it empty.

## [Template — copy this shape for a new entry]

**[Actionable title stating the rule — e.g. "A Widget is never deleted, only archived"]**

[Body: the rule, the reasoning behind it if non-obvious, and a pointer to where it's enforced in code if relevant.]

_Captured YYYY-MM-DD._

---

<!-- Real entries go above this line, newest first. Delete the template entry once you have real content. -->
